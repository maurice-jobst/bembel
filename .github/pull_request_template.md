## Ticket

BEM-XXX — <!-- link the backlog entry -->

## What & why

<!-- Two sentences. If it needs more, the ticket was too big. -->

## Checklist

- [ ] Commit message(s) prefixed with the ticket ID
- [ ] No secrets, team IDs, or API keys in the diff
- [ ] User-facing strings added to `Localizable.xcstrings` (German)
- [ ] `make test` green; `make validate` green if `data/` changed
- [ ] ADR added/updated if this decision is expensive to reverse
