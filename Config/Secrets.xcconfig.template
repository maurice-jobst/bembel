// cp Secrets.xcconfig.template Secrets.xcconfig  — the copy is gitignored.
// CI copies this template verbatim and overrides via repository secrets.

// Your Apple Developer Team ID (Xcode > Settings > Accounts). Leave empty to
// build unsigned for the simulator.
BEMBEL_TEAM_ID =

// RMV Open Data API key (BEM-C01). Register at opendata.rmv.de — approval is
// not instant, do it early. Delivery into the app is a C01 concern.
RMV_API_KEY =
